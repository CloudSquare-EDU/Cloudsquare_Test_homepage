# 줄바꿈 문자를 LF로 통일 (Windows에서 편집해도 CRLF로 저장되지 않도록)
# 이게 없으면 Windows 에디터로 파일을 열기만 해도 git이 파일 전체를 "수정됨"으로
# 표시해서 실제 변경 내용을 알아보기 어려워진다.
* text=auto eol=lf

# 바이너리 파일은 줄바꿈 변환 대상에서 제외
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.xlsx binary
*.xls binary
